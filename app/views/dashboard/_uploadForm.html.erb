	<%= form_for @dook, url: {action: "step3"} do |f| %>
		  <%= f.label(:name , "Title" )  %>
		  <%= f.text_field :name%>
		  <%= f.label :image , "" %>
          <%= f.file_field :image %>
		  <%= f.submit "Create" , class: "btn btn-primary" %>
		<% end %>